language: csharp
mono: latest
dotnet: 2.1.3 # .NET Core
script:
  - ./build.sh
before_install:
  - sudo apt-get install apt-transport-https
  - sudo apt-get update
  - sudo apt-get install -y nuget mono-devel mono-xbuild
  - sudo apt-get install dotnet45

