version: '2.1'
orbs:
  win: circleci/windows@5.0
jobs:
  build:
    executor:
      name: win/server-2022
    steps:
      - checkout
      - run:
          command: .\install-signing-key.bat
          name: Install strong name signing key
      - run: 
          command: .\build.bat
          name: Build